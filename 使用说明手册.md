# 全球交易系统 2.0 使用说明

## 一、正确打开方式

本系统包含一个本地 Node.js 行情服务，不能直接双击 `public/index.html`。

下面这种地址是错误的：

```text
file:///Users/.../public/index.html
```

正确地址必须是：

```text
http://localhost:5173
```

`file://` 只能显示静态页面，无法调用实时行情接口。`http://localhost:5173` 才是完整系统。

## 二、最简单的启动方法

1. 打开文件夹：

   ```text
   /Users/zhoujy/Downloads/信号研究
   ```

2. 双击：

   ```text
   启动全球交易系统.command
   ```

3. 系统会自动启动服务并打开：

   <http://localhost:5173>

4. 启动后不要关闭终端窗口。终端窗口关闭，行情服务也会停止。

## 三、推荐：安装后台常驻

如果不希望每次打开终端，双击：

```text
安装后台常驻.command
```

安装后：

- 登录 macOS 时自动启动。
- 服务意外退出时自动重启。
- 不需要保持终端窗口开启。
- 地址仍然是 <http://localhost:5173>。

如需取消，双击：

```text
卸载后台常驻.command
```

### macOS 首次阻止脚本时

如果提示“无法打开”或“无法验证开发者”：

1. 在 Finder 中右键点击 `启动全球交易系统.command`。
2. 选择“打开”。
3. 在弹出的确认窗口中再次选择“打开”。

## 四、使用终端启动

打开“终端”，执行：

```bash
cd "/Users/zhoujy/Downloads/信号研究"
npm start
```

看到下面文字表示启动成功：

```text
A-share signal monitor: http://localhost:5173
```

然后在浏览器打开：

<http://localhost:5173>

## 五、停止和重新启动

### 停止服务

回到运行服务的终端窗口，按：

```text
Control + C
```

### 重新启动

再次双击 `启动全球交易系统.command`，或者重新执行：

```bash
cd "/Users/zhoujy/Downloads/信号研究"
npm start
```

## 六、常见问题

### 1. 浏览器显示无法连接

原因通常是服务没有启动或已经被关闭。

解决方法：

1. 双击 `启动全球交易系统.command`。
2. 等待终端显示启动成功。
3. 刷新 <http://localhost:5173>。

### 2. 页面能打开，但一直显示“刷新中”

可能是外部行情数据源暂时响应较慢。

解决方法：

1. 等待数秒后点击页面右上角“刷新”。
2. 检查电脑网络。
3. 停止服务后重新启动。

### 3. 端口被占用

在终端执行：

```bash
lsof -nP -iTCP:5173 -sTCP:LISTEN
```

如果显示旧的 Node 进程，可以先关闭原来的终端窗口，或执行：

```bash
pkill -f "node server.js"
```

然后重新启动。

### 4. 提示找不到 npm 或 node

需要安装 Node.js 18 或更高版本：

<https://nodejs.org/>

安装后关闭并重新打开终端，再执行：

```bash
node -v
npm -v
```

两条命令都能显示版本号后，再启动系统。

## 七、日常使用建议

- 将 <http://localhost:5173> 加入浏览器书签。
- 未安装后台常驻时，每次使用前先运行启动脚本。
- 不要直接打开 `public/index.html`。
- 页面默认每 30 秒刷新一次，也可以点击“刷新”。
- 中文输入指数或证券名称，可从候选列表加入监控。
- 关闭电脑或终端后，下次使用需要重新启动服务。

## 八、后台服务日志

如果后台服务无法启动，查看：

```text
/Users/zhoujy/Downloads/信号研究/service.log
/Users/zhoujy/Downloads/信号研究/service-error.log
```

## 九、版本恢复

GitHub 已保存两个稳定版本：

- 1.0 备份：`v1.0-stable`
- 2.0 当前版本：`v2.0.0`

代码仓库：

<https://github.com/zhoujingyu5-arch/a-share-signal-monitor>
